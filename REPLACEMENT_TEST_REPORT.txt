Premier Brushworks JobHub Complete Replacement v5
=================================================

Automated checks completed successfully:

- pb_jobhub_app.py compiled successfully.
- pb_jobhub_visual_scheduler.py compiled successfully.
- A fresh local JobHub database was created.
- The fallback admin login authenticated using the same SHA-256 format as the
  existing JobHub.
- Every manager page rendered without an uncaught exception:
  Dashboard, Jobs, Staff Scheduler, Staff, Timesheets, Materials, Variations,
  Reports, User Access and System.
- Core compatibility tables were present:
  jobs, employees, staff_schedule, app_users, timesheet_entries,
  material_entries, job_variations and staff_leave_requests.
- A test job and staff member appeared in the visual scheduler.
- A schedule assignment was created through the actual scheduler form.
- The saved assignment was verified in staff_schedule with:
  source_app = JobHub Visual Scheduler and created_by = admin.

The automated test used a temporary SQLite database. It did not alter the
user's live Render/PostgreSQL data.
